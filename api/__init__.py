"""FastAPI adapter layer for the existing VISORA BI Python backend.

The package intentionally contains transport concerns only: routing,
request validation, response serialization, and error mapping. Business
logic remains in ``backend/``.
"""
